import { expect, describe, it } from 'vitest';
import '@testing-library/jest-dom';

describe('Setup', () => {
  it('should have testing tools configured', () => {
    expect(true).toBe(true);
  });
});
